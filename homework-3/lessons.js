console.log("list of instruments");
const ul = "list of instruments";

const instrument = ["Piano", "Guitar", "Violin", "Drums", "Saxophone", "Flute"];

console.log(instrument);

for (let i = 0; i < instrument.length; i++) {
    console.log(instrument[i]); // Outputs each number in the array
  }

  